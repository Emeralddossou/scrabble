'use client';import{useState}from'react';import{useRouter}from'next/navigation';import{rpc}from'@/lib/client';export default function Home(){const r=useRouter(),[mode,setMode]=useState<'login'|'register'>('login'),[error,setError]=useState(''),[busy,setBusy]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError('');const f=new FormData(e.currentTarget);try{await rpc(mode,{username:String(f.get('username')),password:String(f.get('password'))});r.push('/dashboard')}catch(x){setError(x instanceof Error?x.message:'Erreur')}finally{setBusy(false)}}return <main className="landing"><section className="hero"><div className="tile-logo"><b>L</b><i>1</i></div><p className="eyebrow">ARÈNE LEXICALE FRANCOPHONE</p><h1>Les mots deviennent<br/><em>des coups de maître.</em></h1><p className="lead">Un Scrabble en ligne précis, tactile, rapide et fiable, même quand le réseau hésite.</p><div className="feature-strip"><span>⚡ Reprise réseau</span><span>♟ Solo & duel</span><span>⌁ Mobile natif</span></div></section><section className="auth-panel"><div className="tabs"><button className={mode==='login'?'active':''} onClick={()=>setMode('login')}>Connexion</button><button className={mode==='register'?'active':''} onClick={()=>setMode('register')}>Créer un compte</button></div><form onSubmit={submit}><label>Nom de joueur<input name="username" required minLength={3} maxLength={24}/></label><label>Mot de passe<input name="password" type="password" required minLength={10}/></label>{mode==='register'&&<small>10 caractères, majuscule, minuscule, chiffre et symbole.</small>}{error&&<p className="error">{error}</p>}<button className="cta" disabled={busy}>{busy?'Patientez…':mode==='login'?'Entrer dans l’arène':'Forger mon profil'}</button></form></section></main>}