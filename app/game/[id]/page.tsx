'use client';import{use,useEffect,useMemo,useState}from'react';import{useRouter}from'next/navigation';import{cached,putCache,rpc}from'@/lib/client';import{multiplierAt}from'@/lib/board';import type{Placement,Tile}from'@/lib/types';type State=any;export default function GamePage({params}:{params:Promise<{id:string}>}){const{id}=use(params),gameId=Number(id),r=useRouter(),[state,setState]=useState<State|null>(null),[placements,setPlacements]=useState<Placement[]>([]),[selected,setSelected]=useState<Tile|null>(null),[message,setMessage]=useState(''),[offline,setOffline]=useState(false);async function load(){try{const s=await rpc<State>('state',{gameId});setState(s);putCache(`game:${gameId}`,s);setOffline(false)}catch{setState(x=>x??cached(`game:${gameId}`));setOffline(true)}}useEffect(()=>{load();const t=setInterval(load,5000);return()=>clearInterval(t)},[gameId]);const me=useMemo(()=>state?.players?.find((p:any)=>p.rack),[state]);function choose(row:number,col:number){if(!selected||state.board[row][col])return;setPlacements(p=>[...p.filter(x=>x.tileId!==selected.id&&!(x.row===row&&x.col===col)),{row,col,tileId:selected.id,letter:selected.letter==='*'?(prompt('Lettre du joker')||'A').slice(0,1).toUpperCase():selected.letter,blank:selected.letter==='*'}]);setSelected(null)}async function act(kind:'pass'|'resign'|'exchange'){try{await rpc('gameAction',{gameId,version:Number(state.version),kind,tileIds:kind==='exchange'&&selected?[selected.id]:[]});setPlacements([]);setSelected(null);await load()}catch(e){setMessage(e instanceof Error?e.message:'Erreur')}}async function submit(){try{const x=await rpc<{score:number;words:string[]}>('play',{gameId,version:Number(state.version),placements});setMessage(`${x.words.join(', ')} : +${x.score} points`);setPlacements([]);await load()}catch(e){setMessage(e instanceof Error?e.message:'Erreur')}}if(!state||!me)return <main className="center-screen">Installation des lettres…</main>;const board=state.board.map((row:any[])=>[...row]);for(const p of placements)board[p.row][p.col]={id:p.tileId,letter:p.letter,points:me.rack.find((t:Tile)=>t.id===p.tileId)?.points||0,blank:p.blank};return <main className="game-shell"><header className="game-top"><button className="quiet" onClick={()=>r.push('/dashboard')}>← Salon</button><div className="scoreboard">{state.players.map((p:any)=><div className={Number(state.current_player_id)===Number(p.user_id)?'turn':''} key={p.user_id}><span>{p.username}</span><b>{p.score}</b></div>)}</div><span className={offline?'network bad':'network'}>{offline?'Hors connexion':'Synchronisé'}</span></header><div className="game-grid"><section className="board-wrap"><div className="board">{board.flatMap((row:any[],ri:number)=>row.map((cell:any,ci:number)=>{const m=multiplierAt(ri,ci),staged=placements.some(p=>p.row===ri&&p.col===ci);return <button key={`${ri}-${ci}`} className={`cell ${m||''} ${staged?'staged':''}`} onClick={()=>choose(ri,ci)}>{cell?<><strong>{cell.letter}</strong><small>{cell.blank?0:cell.points}</small></>:<span>{m==='ST'?'★':m}</span>}</button>}))}</div></section><aside className="game-side"><div className="side-card"><p className="eyebrow">TOUR ACTUEL</p><h2>{Number(state.current_player_id)===Number(me.user_id)?'À vous de composer':'Tour adverse'}</h2><p>{state.bag_count} lettres restantes</p>{message&&<p className="notice">{message}</p>}</div><div className="side-card history"><h3>Historique</h3>{state.moves.slice().reverse().map((m:any)=><div key={m.id}><b>{m.username||'Système'}</b><span>{m.kind==='play'?`${m.words.join(', ')} · +${m.points}`:m.kind}</span></div>)}</div></aside></div><footer className="rack-dock"><div className="rack">{me.rack.map((t:Tile)=>{const used=placements.some(p=>p.tileId===t.id);return <button key={t.id} disabled={used} className={selected?.id===t.id?'selected':''} onClick={()=>setSelected(t)}><strong>{t.letter}</strong><small>{t.points}</small></button>})}</div><div className="game-buttons"><button onClick={submit} disabled={!placements.length||offline}>Valider</button><button className="quiet" onClick={()=>setPlacements([])}>Rappeler</button><button className="quiet" onClick={()=>act('exchange')} disabled={!selected}>Échanger</button><button className="quiet" onClick={()=>act('pass')}>Passer</button><button className="danger" onClick={()=>act('resign')}>Abandonner</button></div></footer></main>}