# Tests

Exécuter `npm ci`, puis `npm run format:check`, `npm run lint`, `npm run typecheck`, `npm run test:unit`, `npm run test:integration`, `npm run build` et `npm run test:e2e`. Les tests d’intégration SQLite utilisent une base en mémoire. La CI lance aussi MySQL via un service GitHub Actions.
